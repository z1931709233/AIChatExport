# Chat Export Toolkit 使用说明

本工具用于将 ChatGPT、腾讯元宝和千问中的聊天记录导出为便于阅读和保存的本地文件。

> 不同平台使用的脚本不同，请根据当前使用的平台启用对应脚本，并关闭其他平台脚本，避免脚本之间相互干扰。

## 支持平台

| 平台 | 平台网址 | 对应脚本 |
| --- | --- | --- |
| ChatGPT | <https://chatgpt.com/> | `userscripts/ChatGPT/chatgpt-export.user.js` |
| 腾讯元宝 | <https://yuanbao.tencent.com/> | `userscripts/元宝/chat-export.v2.user.js` |
| 千问 | <https://www.qianwen.com/> | `userscripts/千问/chat-export.v2.user.js` |

## 使用前准备

- 最新版 Google Chrome 或 Microsoft Edge。
- 已安装并启用 Tampermonkey（篡改猴）。
- 已登录对应平台的网页版。
- “全部会话”以及图片、代码和附件导出需要浏览器的文件夹写入权限，建议使用 Chrome 或 Edge。

## 安装与更新脚本

1. 打开 Tampermonkey 管理面板，选择“添加新脚本”。
2. 删除编辑器中的默认内容。
3. 打开当前平台对应的 `.user.js` 文件，复制全部内容并粘贴到 Tampermonkey。
4. 保存并确认脚本已启用。
5. 关闭已经打开的对应平台标签页，然后重新打开平台网页；也可以使用 `Ctrl+F5` 强制刷新。
6. 页面右下角出现“+”导出按钮后，即可开始使用。

更新脚本时，请用新文件的完整内容覆盖 Tampermonkey 中的旧脚本。不要在同一平台同时启用多个版本的脚本。

## 导出当前会话

1. 打开需要导出的具体对话。
2. 点击页面右下角的“+”导出按钮。
3. 选择“当前会话”。
4. 按页面提示选择导出格式或保存总目录。
5. 点击“导出”并等待完成。

当前会话通常以地址栏中的会话 ID 为准。ChatGPT 和腾讯元宝会将当前会话保存到独立文件夹；千问的当前会话可直接导出为 JSON 或 Markdown，若需要同时下载图片、代码和附件，建议使用“全部会话”导出。

## 导出全部会话

1. 进入对应平台的正常聊天页面，并确认左侧历史会话列表已经加载完成。
2. 点击右下角的“+”导出按钮。
3. 选择“全部会话”，然后点击“导出”。
4. 在浏览器目录选择窗口中选择一个备份总目录，例如：

   ```text
   D:\ChatHistory\ChatGPT
   D:\ChatHistory\元宝
   D:\ChatHistory\千问
   ```

5. 授权网页写入该目录，并保持对应平台页面和浏览器开启，等待进度完成。

导出过程中不要关闭页面、退出登录、清除站点数据，也不要同时在多个标签页启动“全部会话”。历史记录较多时需要等待一段时间。

## 导出目录结构

全部会话导出后，通常会生成如下结构：

```text
备份总目录\
├─ <会话ID>_<时间>_<分组名>\
│  ├─ raw.json
│  ├─ conversation.md
│  ├─ images\
│  │  └─ 图片文件
│  └─ files\
│     └─ 代码及其他附件
└─ metadata.json
```

- `raw.json`：保存会话原始数据及资源信息。
- `conversation.md`：保存便于阅读的聊天内容，图片和附件链接位于对应消息中。
- `images/`：保存聊天中的图片。
- `files/`：保存代码文件及其他可下载附件。
- `metadata.json`：保存导出清单、失败记录等信息。
- 没有可靠分组名称的会话通常归入“其他”。
- Windows 文件名中的非法字符会自动替换，重名目录会自动追加 `_2`、`_3` 等后缀。

## 缓存与中断后继续

部分平台支持缓存和断点续传。导出中断、刷新页面或重新打开浏览器后，可再次选择“全部会话”继续导出。不要清除对应平台的 Cookie、Local Storage、IndexedDB 或其他站点数据，否则可能无法复用缓存。

即使资源下载失败，已成功导出的文本和其他文件通常仍会保留；失败信息可在 `metadata.json` 或 `conversation.md` 中查看。

## 安全与隐私

导出内容可能包含聊天记录、个人信息、图片、附件和源代码。请妥善保存备份目录，不要随意上传、分享或提交到公开仓库。反馈问题时，不要发送 Cookie、Token、完整请求头或未脱敏的 HAR 文件。



## 源仓库与许可证

- 源仓库：<https://github.com/gandli/chat-export-toolkit>
- npm 包：`chat-export-toolkit`
- 原项目许可证：MIT

本仓库中的平台脚本基于上游项目现有架构进行适配和扩展。平台网页接口或服务条款发生变化时，脚本可能需要更新；使用和分发时请遵守对应平台的服务条款及数据使用要求。
